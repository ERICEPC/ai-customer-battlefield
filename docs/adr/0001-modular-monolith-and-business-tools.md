# 模块化单体与业务 Tool seam

系统首期采用模块化单体：客户经营、商机、跟进确认、作战分析、经营动作、报告、通知、身份权限、Agent 运行和配置审计在同一部署内保持独立模块。Agent 只能通过带授权、幂等和审计的业务 Tool interface 读取上下文或提出候选，正式写入由确定性应用模块完成；这样既避免首期微服务的分布式复杂度，也不把业务规则绑定到任何 Agent 框架、模型厂商或客户端。
