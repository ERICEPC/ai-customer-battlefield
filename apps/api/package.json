{
  "name": "@battlefield/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build",
    "dev": "tsx watch src/main.ts",
    "prebuild": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm test:e2e",
    "pretest:e2e": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build",
    "test:e2e": "vitest run test/app.e2e.test.ts"
  },
  "dependencies": {
    "@battlefield/contracts": "workspace:*",
    "@battlefield/core": "workspace:*",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/testing": "^12.0.1",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "tsx": "^4.23.12"
  }
}
