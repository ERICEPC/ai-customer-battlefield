{
  "name": "@battlefield/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build && pnpm --filter @battlefield/database build && pnpm --filter @battlefield/worker build",
    "dev": "tsx watch src/main.ts",
    "dev:demo": "tsx dev/demo-server.ts",
    "prebuild": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build && pnpm --filter @battlefield/database build && pnpm --filter @battlefield/worker build",
    "build": "tsc -p tsconfig.build.json",
    "pretypecheck": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build && pnpm --filter @battlefield/database build && pnpm --filter @battlefield/worker build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm test:e2e",
    "pretest:e2e": "pnpm --filter @battlefield/contracts build && pnpm --filter @battlefield/core build && pnpm --filter @battlefield/database build && pnpm --filter @battlefield/worker build",
    "test:e2e": "node test/run-e2e.mjs",
    "typecheck:dev": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@battlefield/contracts": "workspace:*",
    "@battlefield/core": "workspace:*",
    "@battlefield/database": "workspace:*",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@battlefield/worker": "workspace:*",
    "@nestjs/testing": "^12.0.1",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "tsx": "^4.23.12"
  }
}
